// Observable: a thing that produces values

// Operator: an operation that modifies the data that's being pushed in from the Observable
//  - An operator returns an Observable

// Subscriptions: code that will actually do something with the values produces by the Observables