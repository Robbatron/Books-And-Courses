const colors = ['red', 'green', 'blue'];
const numbers = [1,2,3,4];

for (let color of colors) {
    console.log(color);
}

let total = 0;
for (let number of numbers) {
    total += number;
}
console.log(total);
