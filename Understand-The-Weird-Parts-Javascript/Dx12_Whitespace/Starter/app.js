// Whitespace: Invisible characters that create literal 'space' in your written code
// Ex: Carriage returns, tabs, spaces

var 
	// first name of the person
	firstName,
	// last name of the person
	lastName,
	// the language can be 'en' or 'es'
	language;

var person = {
	// the first name
	firstName: 'Tony',
	// the last name
	// (always required)
	lastName: 'Tiger'
}

console.log(person);